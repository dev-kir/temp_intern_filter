# What Super Filter actually needs to run.
openpyxl==3.1.5          # reads and writes the workbooks
tkinterdnd2==0.6.2       # drag-and-drop onto the window; optional, app runs without it

# Build only, not needed to run from source:
# pyinstaller==6.21.0
